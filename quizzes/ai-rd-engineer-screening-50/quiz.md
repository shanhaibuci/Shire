---
id: ai-rd-engineer-screening-50
title: AI 研发工程师招聘初筛问卷（50题）
description: |
  面向 2-5 年经验候选人的 AI 研发工程师招聘初筛混合问卷。
  本试卷共 50 题，其中单选题 35 题、多选题 10 题、简答题 5 题，预计 64 分钟完成。
  重点考察 LLM 应用工程场景中的模型选型、RAG、Agent、评测、服务化、成本治理、安全合规与故障排查能力。
  题目整体偏工程落地与系统判断，不以算法推导或纯学术记忆为主。
tags: [recruitment, screening, ai, llm, engineering, rag, agent, evaluation]
schema_version: 2
format: qml-v2
question_count: 50
question_counts:
  single: 35
  multiple: 10
  short: 5
estimated_duration_minutes: 64
llm:
  model: gpt-5
  temperature: 0.0
  prompt_template: |
    请根据评分标准对考生答案打分，允许部分得分。
    只输出 JSON，不要解释，不要 Markdown。
    JSON 必须包含字段：score、reason、relevance、contradiction。
    - score：0 到 {{max_points}} 的整数
    - reason：1 到 3 句，说明得分点和失分点
    - relevance：0 到 3 的整数
    - contradiction：true 或 false

    题目：{{question}}
    评分标准：{{rubric}}
    考生回答：{{answer}}
---

## Q1 [single] (2) {answer_time=45s}

要把“用户上传一段很长的招股书并提问关键风险点”做成第一版可用功能，哪种模型能力最值得优先保障？

- A) 只看闲聊风格是否自然
- B*) 足够长上下文、稳定的信息抽取与结构化总结能力
- C) 是否支持生成图片
- D) 是否能在一次回答里写出更多修辞

[rubric]
该任务首先依赖长文档理解、关键信息抽取和结构化输出，B 最符合一版可用能力的优先级。
[/rubric]

## Q2 [single] (2) {answer_time=45s}

你要让模型稳定输出可直接入库的工单分类 JSON。下面哪种做法最稳妥？

- A) 让模型“尽量按 JSON 写”，然后完全依赖正则修补
- B*) 明确定义字段 schema、枚举范围和异常返回约定，再启用结构化输出约束
- C) 增加回答长度，让模型自己形成结构
- D) 只在系统提示里写“不要出错”

[rubric]
结构化输出任务应先定义明确的 schema 和约束，再利用相应机制保证格式稳定，因此 B 正确。
[/rubric]

## Q3 [single] (2) {answer_time=45s}

一个功能需要在“客服问答”“内容改写”“内部分类”三类任务之间动态路由。第一步最合理的工程做法是？

- A*) 先把任务类型、验收标准和失败成本拆开，再决定是否使用不同模型或流程
- B) 统一接入一个最大模型，后续再看是否有问题
- C) 只比较公开榜单分数，不看任务差异
- D) 先把所有任务都改写成同一个 prompt

[rubric]
路由前要先区分任务类型、质量要求和成本约束，否则模型和流程选型没有依据，A 正确。
[/rubric]

## Q4 [single] (2) {answer_time=45s}

你发现模型在抽取合同字段时经常把“未提及”误写成猜测值。最直接有效的 prompt 改进是什么？

- A) 把回答改得更礼貌
- B*) 明确要求“仅依据输入文本提取；无法确认时返回 null 并给出缺失原因”
- C) 只增加 few-shot 数量，不改判空规则
- D) 把温度调高，增加多样性

[rubric]
问题核心在于缺失信息的处理策略，必须显式约束“不确定时返回空值而不是猜测”，因此 B 正确。
[/rubric]

## Q5 [single] (2) {answer_time=45s}

当你需要模型调用外部工具查询库存后再决定是否下单，哪种设计更合理？

- A) 让模型在文本里假装调用工具并自行脑补结果
- B*) 把“决策前必须先查库存”做成可观测的工具调用步骤，再根据真实返回继续流程
- C) 让模型先给结论，库存不足时再人工补救
- D) 完全依赖 prompt 里描述的库存常识

[rubric]
工具依赖型任务必须把真实查询结果纳入流程，避免模型臆造工具结果，B 正确。
[/rubric]

## Q6 [single] (2) {answer_time=45s}

某功能每天请求量很大，但只有少数长尾请求需要高质量推理。最合理的默认架构是？

- A) 所有请求一律走最强模型
- B*) 按任务复杂度分层路由，简单请求优先走便宜快速模型，复杂请求再升级
- C) 只让用户自己选择模型
- D) 先不做区分，等预算超了再整体降配

[rubric]
高频场景下的常见工程解法是按复杂度分层路由，兼顾成本和质量，B 正确。
[/rubric]

## Q7 [single] (2) {answer_time=45s}

RAG 问答经常引用到同一份文档里不相干的段落，最值得优先检查的项是什么？

- A*) 文档切分粒度和片段是否保留了完整语义边界
- B) 页面配色是否影响模型判断
- C) 是否把 temperature 调到 0
- D) 是否把系统提示写得更长

[rubric]
引用错位通常先从切分策略和片段语义完整性排查，A 最直接。
[/rubric]

## Q8 [single] (2) {answer_time=45s}

一个知识库更新频繁，但用户问题集中在最近两周内的新政策。下面哪种方案更合理？

- A) 对模型做一次长期微调即可，不必考虑数据更新
- B*) 优先建设可增量更新的索引和时间感知检索策略，再决定是否需要额外训练
- C) 每次把所有文档全文塞进 prompt
- D) 让标注同学每天手动总结最新政策给模型记住

[rubric]
频繁更新的知识更适合通过可更新索引和检索策略管理，B 正确。
[/rubric]

## Q9 [single] (2) {answer_time=45s}

你发现召回结果里经常混入历史废弃文档。要优先解决这个问题，最有效的措施是？

- A*) 在索引和检索过滤条件中显式加入版本状态、有效期或文档生效标记
- B) 单纯增大 top_k
- C) 让模型自己判断哪些文档可能过时
- D) 把回答长度限制得更短

[rubric]
废弃文档混入首先是索引元数据和过滤条件问题，应在检索层显式处理，A 正确。
[/rubric]

## Q10 [single] (2) {answer_time=45s}

某问答系统“能答出来”，但经常不给出处，导致业务方不敢用。最应该优先补什么？

- A*) 让返回结果强制包含引用片段或来源标识，并在缺少依据时显式拒答
- B) 把回答写得更像人工客服
- C) 让模型一次生成更长解释
- D) 只在 UI 上加一行“内容仅供参考”

[rubric]
业务可用性的关键是可追溯依据，不是文风。应优先补引用和拒答机制，因此 A 正确。
[/rubric]

## Q11 [single] (2) {answer_time=45s}

你怀疑召回质量差不是 embedding 模型本身的问题，而是 query 改写过度。哪种验证思路更合理？

- A*) 保留原始 query、改写后 query、召回结果和最终答案，做对照评估
- B) 直接更换 embedding 模型，不做对照
- C) 只看最终答案是否流畅
- D) 把 chunk 大小改成固定 512 后停止排查

[rubric]
要定位改写问题，必须保留改写前后链路并做对照，A 正确。
[/rubric]

## Q12 [single] (2) {answer_time=45s}

若用户问题明显超出知识库边界，RAG 系统最稳妥的默认行为是？

- A) 让模型自由发挥，尽量别冷场
- B*) 明确说明当前检索不到足够依据，并提示补充问题或转人工
- C) 用训练时的通用知识先凑一个答案
- D) 把 top_k 从 5 调到 50 一定能解决

[rubric]
知识边界外问题应优先暴露“不足以回答”的状态，而不是幻觉补全，B 正确。
[/rubric]

## Q13 [single] (2) {answer_time=45s}

同一个知识点被拆在多个相邻 chunk 中，导致引用片段各自都不完整。下面哪种优化更值得优先尝试？

- A*) 在切分时引入适度 overlap，并按标题/段落等结构保留上下文
- B) 只提高回答 token 上限
- C) 让模型在回答前先写一段自我反思
- D) 把所有 chunk 都合并成一整本书

[rubric]
相邻片段上下文断裂通常通过合理 overlap 和结构化切分来改善，A 正确。
[/rubric]

## Q14 [multiple] (3) {partial=true, answer_time=75s}

在排查 RAG 召回差的问题时，哪些日志或中间结果最值得优先保留？

- A*) 原始 query 与改写后的 query
- B*) 检索返回的 chunk 列表及其分数
- C*) 最终回答所引用的片段 ID 或来源
- D) 只有最终回答正文就足够
- E*) 触发过滤器后的候选文档数量变化

[rubric]
排查 RAG 问题需要保留 query、召回结果、引用链路和过滤过程。A、B、C、E 正确；D 不足以定位问题。
[/rubric]

## Q15 [multiple] (3) {partial=true, answer_time=75s}

下面哪些做法通常有助于提升 RAG 系统的可信度，而不只是“看起来更聪明”？

- A*) 要求答案绑定引用依据
- B*) 对知识库边界外问题设置拒答或转人工策略
- C) 为了显得完整，优先补足缺失事实
- D*) 对过期或低可信文档加过滤规则
- E*) 在评测中单独统计“有依据但答错”和“无依据乱答”

[rubric]
可信度建设依赖引用、边界控制、文档治理和更细粒度评测。A、B、D、E 正确；C 会放大幻觉风险。
[/rubric]

## Q16 [short] {max=8, answer_time=5m}

某内部知识问答系统最近一周投诉变多，表现为“能搜到相关文档，但回答经常引用不稳、关键结论漏掉、同一问题前后回答不一致”。请给出你的排查顺序和改进方案。

[rubric]
1) 能把问题拆到检索、切分、query 改写、重排、生成、引用绑定等多个链路环节，而不是笼统归因。
2) 给出可执行的排查顺序，如先复现样本、看召回日志、核对 chunk 与引用、检查 prompt 和拒答策略。
3) 能提出至少两类改进措施，如数据清洗与切分调整、排序优化、引用约束、评测集补充、日志补全。
4) 能体现工程验证意识，如做前后对比、建立失败样本集、分类型统计问题。
[/rubric]

## Q17 [single] (2) {answer_time=45s}

你要把“查订单状态、判断是否超时、再决定是否补偿”做成 Agent 流程。最不应该缺少的设计是什么？

- A) 让模型完全凭历史对话自行判断订单状态
- B*) 把关键外部事实获取、规则判断和最终执行动作拆成明确步骤，并为高风险动作设置人工确认
- C) 让模型一次性输出全部结论和执行结果
- D) 只要模型能解释思路，就不需要工具调用日志

[rubric]
带业务动作的 Agent 流程必须拆出真实取数、规则判断和高风险确认点，B 正确。
[/rubric]

## Q18 [single] (2) {answer_time=45s}

一个 Agent 经常在工具调用失败后反复重试，造成外部服务雪崩。最值得优先加入什么机制？

- A*) 明确的重试上限、超时控制、幂等设计和失败降级路径
- B) 更长的思维链提示
- C) 更大的上下文窗口
- D) 更高的 temperature

[rubric]
该问题本质是流程控制和稳定性设计不足，应优先补重试、超时、幂等和降级机制，A 正确。
[/rubric]

## Q19 [single] (2) {answer_time=45s}

你发现 Agent 在多步任务里经常“计划很合理，但调用了错误工具”。最可能先要补的是什么？

- A) 更多鼓励性提示语
- B*) 更严格的工具描述、输入参数约束和可调用条件说明
- C) 更长的最终输出模板
- D) 让模型先回答“我很确定”

[rubric]
错误工具选择通常和工具语义边界不清、参数约束不明确有关，B 正确。
[/rubric]

## Q20 [single] (2) {answer_time=45s}

一个 Agent 需要处理退款申请，其中一部分请求会直接影响资金流。最合理的默认策略是？

- A) 让 Agent 自主决定并直接执行全部退款
- B*) 让 Agent 完成信息收集、规则判断和建议生成，真正的资金动作走人工或受控审批
- C) 只要用户语气强烈就优先退款
- D) 先上线再看有没有事故

[rubric]
涉及资金和高风险操作时，应保留人工或受控审批，B 正确。
[/rubric]

## Q21 [single] (2) {answer_time=45s}

当 Agent 需要跨多个系统执行任务时，哪种做法更利于后续排障和审计？

- A*) 为每一步保留输入、工具返回、决策摘要和 trace ID
- B) 只记录最终输出给用户的那段文本
- C) 只保留成功流程，不记录失败流程
- D) 把所有中间信息都直接丢给最终模型，不做存档

[rubric]
多系统 Agent 需要完整链路可观测性，A 正确。
[/rubric]

## Q22 [single] (2) {answer_time=45s}

团队想快速做一个“会自动执行操作”的 Agent 原型。作为工程负责人，最需要先拦住的风险是什么？

- A) UI 是否足够炫
- B*) 没有明确动作边界、回滚方案和人工接管机制就直接放开执行权限
- C) 提示词不够长
- D) 是否已经支持多语言

[rubric]
自动执行类 Agent 的首要风险是权限与回滚不可控，B 正确。
[/rubric]

## Q23 [multiple] (3) {partial=true, answer_time=75s}

下面哪些设计通常是生产级 Agent 工作流里值得保留的？

- A*) 工具调用的参数校验
- B*) 关键步骤的超时与重试策略
- C*) 高风险动作的人工确认或双重校验
- D) 让模型自由决定是否跳过日志
- E*) 每一步的可观测 trace 或事件记录

[rubric]
生产级 Agent 需要参数校验、超时重试、人工确认和可观测链路。A、B、C、E 正确；D 不合理。
[/rubric]

## Q24 [multiple] (3) {partial=true, answer_time=75s}

如果一个 Agent 的任务成功率不稳定，下面哪些分析维度值得优先切分查看？

- A*) 按任务类型或子流程阶段分开统计
- B*) 按工具失败、模型判断错、外部数据缺失等失败类型分开统计
- C) 只看整体平均成功率
- D*) 按输入复杂度、上下文长度或用户群体分层
- E*) 保留典型失败样本做复盘

[rubric]
定位成功率不稳定，需要按任务、失败类型、输入层次和样本复盘做细分分析。A、B、D、E 正确；C 过于粗糙。
[/rubric]

## Q25 [short] {max=8, answer_time=5m}

请设计一个“帮助客服处理退款请求”的 Agent 工作流。要求说明：哪些步骤交给模型，哪些步骤必须走工具，哪些节点必须人工确认，以及你会如何控制误操作风险。

[rubric]
1) 明确区分模型负责的理解/归纳任务与工具负责的事实查询/执行任务。
2) 能指出至少一个必须人工确认的高风险节点，如资金动作、越权例外、规则冲突。
3) 体现参数校验、权限控制、幂等、日志与回滚等风险控制措施。
4) 流程顺序清晰，不把关键事实或执行结果交给模型臆造。
[/rubric]

## Q26 [single] (2) {answer_time=45s}

你要评估“生成商品卖点摘要”的功能是否可上线，第一步最合理的是？

- A*) 先定义代表性样本、评分标准和失败类型，再做离线评测
- B) 先让几位同事随便看几条，觉得不错就上线
- C) 先优化 UI，评测以后再补
- D) 只看模型供应商的 benchmark 宣传材料

[rubric]
上线前评测首先要定义样本、标准和失败类型，A 正确。
[/rubric]

## Q27 [single] (2) {answer_time=45s}

某功能在线 CTR 提升了，但投诉率也明显上升。评估该改动是否成功时，哪种结论最合理？

- A) CTR 提升就说明一定成功
- B*) 不能只看单一正向指标，需要结合投诉、误导率或人工复核结果综合判断
- C) 投诉一定与模型无关
- D) 先把投诉入口关掉再观察

[rubric]
线上效果必须综合质量与风险指标，不能只看 CTR，B 正确。
[/rubric]

## Q28 [single] (2) {answer_time=45s}

离线评测分数很好，但上线后用户满意度下降。下面哪种原因最值得优先怀疑？

- A*) 评测集和真实流量分布不一致，或验收标准没有覆盖关键失败模式
- B) 一定是模型突然变笨了
- C) 只可能是前端页面颜色变了
- D) 说明离线评测本身毫无意义

[rubric]
离线与线上不一致通常先怀疑评测代表性和指标覆盖不足，A 正确。
[/rubric]

## Q29 [single] (2) {answer_time=45s}

团队在做 prompt 改版 A/B 实验，但同时换了模型、检索策略和前端文案。这个实验设计最大的问题是什么？

- A*) 同时改动过多变量，无法判断效果差异来自哪里
- B) 样本量可能太大
- C) prompt 不应该做 A/B
- D) 实验必须在晚上做才准确

[rubric]
同时改太多变量会让归因失效，A 正确。
[/rubric]

## Q30 [single] (2) {answer_time=45s}

你要为“事实问答”设计验收标准。下面哪项最不该缺？

- A) 回答是否显得热情
- B*) 事实正确率、引用一致性和无依据回答率
- C) 是否总能回答得很长
- D) 是否每次都包含表情

[rubric]
事实问答的核心验收指标应围绕正确性、依据一致性和幻觉控制，B 正确。
[/rubric]

## Q31 [multiple] (3) {partial=true, answer_time=75s}

构建一套 LLM 功能评测集时，哪些样本维度值得主动覆盖？

- A*) 常规成功样本
- B*) 高风险失败样本和边界样本
- C*) 易混淆但表面相似的对抗样本
- D) 只保留最容易得高分的样本
- E*) 不同长度、不同来源质量和不同用户表达方式的样本

[rubric]
好的评测集应覆盖常规、边界、对抗和分布差异样本。A、B、C、E 正确；D 会让评测失真。
[/rubric]

## Q32 [multiple] (3) {partial=true, answer_time=75s}

如果团队想让评测结果更能指导迭代，而不是停留在一个总分上，哪些做法更合理？

- A*) 给失败样本打标签，区分召回错、理解错、格式错、越权错等类型
- B*) 保留基线版本并做前后对比
- C) 只保留一个综合分，不分析细项
- D*) 对不同业务场景分别看指标
- E*) 把典型 bad case 沉淀为固定回归集

[rubric]
可指导迭代的评测体系需要失败分类、基线对照、分场景分析和回归集。A、B、D、E 正确；C 不足。
[/rubric]

## Q33 [short] {max=8, answer_time=5m}

请设计一套用于评估“基于知识库回答内部制度问题”的评测方案。至少说明：样本怎么选、指标看什么、如何区分检索问题和生成问题、上线前后的评测如何衔接。

[rubric]
1) 说明样本覆盖思路，如常见问题、边界问题、易混淆问题、过期文档问题。
2) 指标不止一个总分，至少包含正确性、引用/依据、拒答或幻觉控制中的两项。
3) 能提出区分检索与生成问题的方法，如保留召回结果、分阶段打点、错误分类。
4) 能说明上线前离线评测与上线后抽样、监控或回归集之间的衔接。
[/rubric]

## Q34 [single] (2) {answer_time=45s}

某接口 P95 延迟突然从 3 秒升到 12 秒，但请求量没有明显变化。排查时最值得优先看什么？

- A*) 各阶段耗时拆分，如检索、模型推理、外部工具调用和后处理
- B) 先给用户发公告说“稍后再试”
- C) 只看最终返回文案是否变长
- D) 先把 temperature 调低

[rubric]
延迟问题应先拆链路耗时，定位瓶颈所在，A 正确。
[/rubric]

## Q35 [single] (2) {answer_time=45s}

一个功能的 token 成本持续超预算，但质量并没有同步提升。最合理的优化方向是？

- A*) 先识别哪些请求真的需要高成本推理，再做缓存、路由和上下文裁剪
- B) 统一把所有回答长度翻倍
- C) 所有请求都改走最大模型，保证“值回票价”
- D) 完全停止记录成本明细

[rubric]
成本治理首先要做请求分层和结构性优化，如缓存、路由与上下文治理，A 正确。
[/rubric]

## Q36 [single] (2) {answer_time=45s}

你要给常见重复问答做缓存。为了避免错误命中，哪种设计更稳妥？

- A) 只按用户 ID 做缓存键
- B*) 结合归一化后的请求特征、知识版本或配置版本设计缓存键
- C) 把所有请求都写死成同一个缓存键
- D) 完全忽略提示词和知识版本变化

[rubric]
缓存命中既要提高复用，也要避免跨版本误命中，因此 B 正确。
[/rubric]

## Q37 [single] (2) {answer_time=45s}

流量高峰时，外部 rerank 服务偶发超时，导致整体接口大量失败。哪种策略更合理？

- A*) 设置超时降级路径，如回退到基础检索结果，并记录受影响请求
- B) 无限等待 rerank 返回，保证路径一致
- C) 直接关闭全部服务
- D) 让模型自己猜 rerank 结果

[rubric]
外部依赖不稳时，应优先考虑超时降级和可观测处理，A 正确。
[/rubric]

## Q38 [single] (2) {answer_time=45s}

为了支持多个模型供应商切换，哪种工程做法更有利于后续维护？

- A*) 抽象统一的调用接口、错误语义和配置层，再在适配层处理各家差异
- B) 在业务代码里到处写各家 SDK 的细节判断
- C) 先写死一个供应商，未来再大改
- D) 让前端直接调用各家模型接口

[rubric]
多供应商场景应通过统一抽象和适配层隔离差异，A 正确。
[/rubric]

## Q39 [single] (2) {answer_time=45s}

系统偶尔返回空响应，但日志里只有“请求失败”。为提高可排障性，最该补什么？

- A*) 分阶段错误码、请求上下文、上游依赖状态和关联 trace 信息
- B) 只把日志级别从 info 改成 debug
- C) 把失败提示写得更委婉
- D) 只记录最终 HTTP 状态码 500

[rubric]
要排查空响应，必须补全分阶段错误语义与链路上下文，A 正确。
[/rubric]

## Q40 [multiple] (3) {partial=true, answer_time=75s}

在做 AI 服务成本治理时，下面哪些手段通常值得优先纳入？

- A*) 按请求复杂度路由不同模型
- B*) 对稳定结果做缓存或复用
- C*) 监控 token、请求量、失败重试和各模型成本明细
- D) 为了统一，所有请求只走一个最贵模型
- E*) 对上下文做裁剪、摘要或分段处理

[rubric]
成本治理通常依赖分层路由、缓存、细粒度监控和上下文治理。A、B、C、E 正确；D 不合理。
[/rubric]

## Q41 [multiple] (3) {partial=true, answer_time=75s}

为了提高 AI 服务在生产环境中的稳定性，下面哪些设计更合理？

- A*) 对外部依赖设置超时、熔断或降级
- B*) 为关键流程保留告警和健康检查
- C) 只在本地环境做手工测试
- D*) 对关键配置和 prompt 变更做版本管理
- E*) 通过灰度或小流量验证控制发布风险

[rubric]
生产稳定性需要依赖降级、监控、版本管理和灰度发布。A、B、D、E 正确；C 不足。
[/rubric]

## Q42 [short] {max=8, answer_time=5m}

某 AI 功能最近两周 P95 延迟和 token 成本同时上涨，但线上质量指标没有提升。请写出你的优化优先级和验证方法。

[rubric]
1) 能从链路耗时、模型路由、上下文长度、重试、缓存、外部依赖等方面提出排查与优化项。
2) 优先级排序合理，先做高收益低风险的结构性优化，而不是笼统“全部降模型”。
3) 说明如何验证优化有效，如前后对比、灰度、分层统计、成本与质量双指标观察。
4) 体现稳定性意识，如避免因降成本直接破坏高风险场景质量。
[/rubric]

## Q43 [single] (2) {answer_time=45s}

有同事提议把包含用户身份证号和病史的原始文本直接发给外部模型做摘要。最合理的处理是？

- A) 只要模型够强就可以直接发
- B*) 先确认合规边界和数据权限，优先脱敏、最小化传输，必要时改用合规环境或拒绝该方案
- C) 把数据拆成两段发就不算敏感数据
- D) 只在测试环境这样做就没问题

[rubric]
涉及高敏感数据时，应先满足合规、权限和最小化原则，B 正确。
[/rubric]

## Q44 [single] (2) {answer_time=45s}

上线后发现系统偶尔会引用测试环境文档回答正式用户问题。最值得优先修复的是什么？

- A*) 数据源隔离、索引边界和环境级访问控制
- B) 把回答语气调得更谨慎
- C) 增加更多营销文案
- D) 缩短回答长度

[rubric]
该问题本质是环境隔离和访问控制失效，应先修复数据边界，A 正确。
[/rubric]

## Q45 [single] (2) {answer_time=45s}

某次改版后，模型开始在没有依据时也给出“看起来很像真的”答案。为了尽快止损，最合理的第一步是？

- A*) 回滚高风险改动或切回稳定版本，同时保留失败样本做复盘
- B) 继续观察几天，看看用户是否能适应
- C) 只在群里提醒同事先别用
- D) 把回答开头统一加上“仅供参考”

[rubric]
高风险质量回退应优先止损和保留样本，A 正确。
[/rubric]

## Q46 [single] (2) {answer_time=45s}

团队准备把一个 prompt 从人工维护改成配置中心动态下发。哪种附带措施最不该省略？

- A) 只允许口头通知改了什么
- B*) 配置版本管理、灰度发布和快速回滚能力
- C) 让所有环境共用同一个实时配置
- D) 只在发生事故后再补记录

[rubric]
动态配置会提升变更风险，必须配套版本、灰度和回滚能力，B 正确。
[/rubric]

## Q47 [single] (2) {answer_time=45s}

你在值班时收到告警：某核心问答接口“成功率正常”，但人工抽查发现答案质量明显变差。下面哪种认识最合理？

- A) 成功率正常就说明系统没问题
- B*) AI 系统不能只看可用性，还要补充质量监控、抽样复核或关键业务指标
- C) 质量问题无法监控，只能靠运气
- D) 先关掉人工抽查，避免看到负面样本

[rubric]
AI 系统既要看可用性，也要看质量与风险指标，B 正确。
[/rubric]

## Q48 [multiple] (3) {partial=true, answer_time=75s}

为了降低生产环境中的数据和权限风险，下面哪些措施通常合理？

- A*) 对输入数据做脱敏和最小必要传输
- B*) 对工具调用和数据访问做权限校验
- C*) 区分测试、预发、生产环境的数据源和密钥
- D) 为方便排查，把全部原始敏感日志长期明文保存到共享群文件
- E*) 对高风险操作保留审计记录

[rubric]
风险控制依赖脱敏、权限、环境隔离和审计。A、B、C、E 正确；D 明显不当。
[/rubric]

## Q49 [multiple] (3) {partial=true, answer_time=75s}

如果你要为 AI 功能建立上线后的观测体系，下面哪些维度值得纳入？

- A*) 接口成功率、延迟和成本
- B*) 幻觉/无依据回答、人工兜底率或投诉率
- C*) 关键版本、prompt 或检索配置变更记录
- D) 只看 PV 和 UV
- E*) 典型 bad case 的持续回归结果

[rubric]
AI 观测体系应覆盖基础可用性、质量风险、变更和回归表现。A、B、C、E 正确；D 过于单薄。
[/rubric]

## Q50 [short] {max=8, answer_time=5m}

某面向正式用户的 AI 问答功能上线后，出现了“少量但高风险”的错误回答，且暂时无法快速复现。请写出你的应急处理、定位思路和后续防再发措施。

[rubric]
1) 能先写出止损动作，如降级、回滚、限制场景、人工兜底或关闭高风险入口。
2) 有明确的定位思路，如补充链路日志、收集失败样本、按版本与流量分层、排查检索与生成链路。
3) 能提出防再发措施，如回归集、灰度、质量监控、引用约束、评测补充、变更管理。
4) 体现工程与风险意识，不把问题简单归因为“再调 prompt”。
[/rubric]
